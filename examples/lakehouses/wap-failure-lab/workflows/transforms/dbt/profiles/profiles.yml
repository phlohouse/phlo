wap_failure_lab:
  target: dev
  outputs:
    dev:
      type: trino
      method: none
      user: dagster
      host: "{{ env_var('TRINO_HOST', 'trino') }}"
      port: "{{ env_var('TRINO_PORT', '8080') | int }}"
      catalog: iceberg
      schema: raw
      http_scheme: http
      threads: 2
