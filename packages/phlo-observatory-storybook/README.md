# Observatory Storybook

Isolated stories for shared UI. Imports tokens from `../phlo-observatory/web/src/styles.css`.

```bash
npm install
npm run storybook
```

Stories: Button, Card, Badge, DataTable, Inspector, Stat, FilterBar — each
with 2–3 variants driven by `demo.ts` data.
